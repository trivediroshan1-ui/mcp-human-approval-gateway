PORT=4174
DATABASE_PATH=./data/gateway.db

# Optional OpenAI-compatible model endpoint. If omitted, the gateway uses a
# deterministic offline analyst so the demo remains reproducible and safe.
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=
